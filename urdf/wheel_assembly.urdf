<?xml version="1.0"?>
<robot name="wheel" xmlns:xacro="http://ros.org/wiki/xacro">

  <xacro:property name="configuration_file" value="$(find simulator)/config/car_parameters.yaml"/>
  <xacro:property name="parameters" value="${xacro.load_yaml(configuration_file)}"/>

  <xacro:property name="scale" value="0.001"/>

  <xacro:property name="wheel_mass" value="${parameters['wheel']['mass']}"/>
  <xacro:property name="wheel_friction_mu1" value="${parameters['wheel']['mu_longitudinal']}"/>
  <xacro:property name="wheel_friction_mu2" value="${parameters['wheel']['mu_lateral']}"/>
  <xacro:property name="wheel_friction_kp"  value="${parameters['wheel']['contact_stiffness']}"/>

  <xacro:property name="wheel_radius" value="${parameters['wheel']['diameter']/2}"/>
  <xacro:property name="wheel_width" value="${parameters['wheel']['width']}"/>
  <xacro:property name="wheel_base" value="${parameters['appearance']['wheelbase']}"/>
  <xacro:property name="wheel_y" value="${parameters['wheel']['wheel_y']}"/>
  <xacro:property name="wheel_z" value="${parameters['wheel']['wheel_z']}"/>


  <xacro:property name="wheel_z_offset" value="0.221"/>

  <xacro:property name="chassis_width" value="1.15"/>
  <xacro:property name="wheel_ygap" value="0.035"/>

  <xacro:macro name="cylinder_inertia" params="m r h">
    <inertial>
      <origin xyz="0 0 0" rpy="${pi/2} 0 0" />
      <mass value="${m}"/>
      <inertia ixx="${(m/12) * (3*r*r + h*h)}" ixy = "0" ixz = "0" iyy="${(m/12) * (3*r*r + h*h)}" iyz = "0" izz="${(m/2) * (r*r)}"/>
    </inertial>
  </xacro:macro>
 

  <xacro:macro name="wheel" params="prefix is_rear is_left reverse:=1">
    <link name="${prefix}_link">
      <visual>
        <origin xyz="0 0 0" rpy="${-is_left*pi + pi/2} 0 0"/>
        <geometry>
          <mesh filename="file://$(find simulator)/meshes/lrt4/wheel_assembly/wheel.stl" scale="${scale} ${scale} ${scale}"/>
        </geometry>
        <material name="Gray">
          <color rgba="0.5 0.5 0.5 1.0"/>
        </material>
      </visual>
      <visual>
        <origin xyz="0 ${-wheel_width/2} 0" rpy="0 0 0"/>
        <geometry>
          <mesh filename="file://$(find simulator)/meshes/lrt4/wheel_assembly/tire.stl" scale="${scale} ${scale} ${scale}"/>
        </geometry>
        <material name="Gray">
          <color rgba="0.5 0.5 0.5 1.0"/>
        </material>
      </visual>
 
      <collision>
        <origin xyz="0 0 0" rpy="0 0 0"/> 
        <geometry>
          <mesh filename="file://$(find simulator)/meshes/lrt4/wheel_assembly/tire.stl" scale="${scale} ${scale} ${scale}"/>
        </geometry>
      </collision>

      <xacro:cylinder_inertia m="${wheel_mass}" r="${wheel_radius}" h="${wheel_width}"/>

    </link>

    <gazebo reference="${prefix}_${is_left}_wheel">
      <material>Gazebo/Black</material>
      <mu1>${wheel_friction_mu1}</mu1>
      <mu2>${wheel_friction_mu2}</mu2>
      <kp>${wheel_friction_kp}</kp>
    </gazebo>

    <!-- MOTOR -->

    <link name="${prefix}_motor">
      <visual>
        <origin xyz="0.0 0.0 0.0" rpy="0.0 0.0 ${reverse*pi/2}"/>
        <geometry>
          <mesh filename="file://$(find simulator)/meshes/lrt4/wheel_assembly/motor.stl" scale="${scale} ${scale} ${scale}"/>
        </geometry>
      </visual>
      <visual>
        <origin xyz="0.0 0.0 0.0" rpy="0.0 ${pi/2} ${-reverse*pi/2}"/>
        <geometry>
          <mesh filename="file://$(find simulator)/meshes/lrt4/wheel_assembly/cooling_jacket.stl" scale="${scale} ${scale} ${scale}"/>
        </geometry>
      </visual>
    </link>

    <joint name="${prefix}_motor_joint" type="continuous">
      <parent link="chassis"/>
      <child link="${prefix}_motor"/>
      <origin xyz="${-is_rear*wheel_base} ${-wheel_y + 2*is_left*wheel_y} ${wheel_z}" rpy="0 0 0"/>
      <axis xyz="0 1 0"/>
    </joint>

    <joint name="${prefix}_joint" type="fixed">
      <parent link="${prefix}_motor"/>
      <child link="${prefix}_link"/>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <axis xyz="0 1 0"/>
    </joint>
    

  </xacro:macro>


  
</robot>
